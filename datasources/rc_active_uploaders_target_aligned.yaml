id: rc_active_uploaders_target_aligned
name: Active Uploaders (1+) (Aligned with Targets)
shortName : Active Uploaders (Aligned with Targets)
format: csv
type: timeseries
url: /data/datafiles/rc/rc_active_uploaders_target_aligned.csv

columns:
    -   id         : month
        label      : Month
        type       : date
    
    -   id         : commons
        label      : Total (Actual)
        type       : int
        metricRefs : upload
    
    -   id         : commons_target
        label      : Target (Annual Plan)
        type       : int
        metricRefs : upload
    
